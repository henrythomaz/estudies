// Call Backs
// forEach

//      forEach é um método de manipulção de arrays que espera um call back e retorna uma função para cada propriedade do array

    const names = ["Henry", "Heitor", "Isis", "Italo", "Lázaro", "Leonardo", "Arthur", "Alvaro"]

    names.forEach(item => {
        console.log(item)
    })
